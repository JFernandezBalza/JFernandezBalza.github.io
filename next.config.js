// next.config.js

const nextConfig = {
  // Aquí puedes añadir tus opciones de configuración si las necesitas.
  // Por ahora, lo dejaremos vacío como estaba.
};

module.exports = nextConfig; // Cambiamos 'export default' por 'module.exports'
